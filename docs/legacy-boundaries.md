# Legacy Boundaries

`App/` is legacy. It is not part of the active solution, build, or current architecture decisions.

Rules for maintenance:
- Do not modify `App/` as part of active feature or refactor work.
- Do not use `App/` as reference for the active `Backup.*` projects.
- Treat `Backup.*` projects as the active codebase.
